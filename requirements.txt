fastapi>=0.110.0
uvicorn>=0.27.0
lunar-python>=1.4.8
pyswisseph>=2.10
jinja2>=3.1.0
python-multipart>=0.0.9
pyyaml>=6.0
httpx>=0.27.0

# 八术引擎依赖
iztro-py>=2.0.0
najia>=0.3.0
kintaiyi>=0.2.0
kinliuren>=0.1.0

# kintaiyi 的传递依赖
# 注意: taiyidict 是 kintaiyi 内部子模块,不通过 pip 单独安装,
# 而是 kintaiyi 包内自带 (lib/taiyidict.py),由 engine/taiyi_engine.py 用
# importlib 手动注册后加载. 见 commit 61c7202 的修复.
numpy>=1.24.0
cn2an>=0.5.0
sxtwl>=1.0.0

# LLM 客户端（可选，离线时自动回退规则引擎）
openai>=1.0.0

# Dev / Test
pytest>=7.0
pytest-asyncio>=0.21.0
playwright>=1.40.0
ruff>=0.1.0